/**
 * @deprecated Use `@/lib/ikazin/tokens` em código novo.
 *
 * Este arquivo é mantido apenas por compatibilidade. Todos os exports
 * delegam para tokens.ts (Design System 2.0). Removido em sprint futura.
 */

export {
  IKAZIN_DESIGN,
  TIER_CONFIG,
  TOTAL_BUILDS,
  getTierForBuild,
  type BuildTier,
} from './tokens'
